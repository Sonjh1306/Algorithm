import Foundation

class WordFlipping2 {
    func solution()  {
        var input = readLine()!.map{ String($0) }
        var result: String = ""
        
        while !input.isEmpty {
            
            var basket = ""
            
            for index in input {
                if index == "<" {
                    basket += index
                } else if index == ">" {
                    
                }
            }
        }
        
    }
}
